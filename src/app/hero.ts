export class Hero {
    public party: boolean = false;
    constructor(public name: string, public property: string, public type: string, public terr1: string, public imgurl: string, public id: number) { }
}